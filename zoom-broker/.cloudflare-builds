# Cloudflare Builds Configuration Reference
# This file documents the build settings configured in the Cloudflare Dashboard
# It is NOT read by Cloudflare - it's here for documentation purposes only

# Build Configuration (set in Dashboard → Settings → Builds)
PRODUCTION_BRANCH=main
ROOT_DIRECTORY=/zoom-broker
BUILD_COMMAND=npm ci && npm test && npx wrangler deploy

# Note: The actual configuration must be set via the Cloudflare Dashboard web UI
# when connecting your GitHub repository. This file serves as documentation
# for team members to understand the expected build settings.

